#include<stdio.h>
//dp使わずに解くか

longlong int kyoka(long long int x) {
	if (x==0) return 1;	//0なら、0のみ許可されているので、1個#include<stdio.h>
//dp使わずに解くか

longlong int kyoka(long long int x) {
	if (x==0) return 1;	//0なら、0のみ許可されているので、1個#include<stdio.h>
//dp使わずに解くか

longlong int kyoka(long long int x) {
	if (x==0) return 1;	//0なら、0のみ許可されているので、1個
